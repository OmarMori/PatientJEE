package ma.emsi.patientmvc.entities;import jakarta.persistence.*;import jakarta.validation.constraints.DecimalMin;import jakarta.validation.constraints.NotEmpty;import jakarta.validation.constraints.Size;import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;import org.springframework.format.annotation.DateTimeFormat;import java.io.Serializable;import java.util.Collection;import java.util.Date;@Entity //entite persistante dans la bd@Data //ajouter lesmethodes auto getters et setters  et tostring...@NoArgsConstructor //Constructeur sans param@AllArgsConstructor //Constructeur avec parampublic class Patient implements Serializable {    @Id //indique cle primaire    @GeneratedValue(strategy = GenerationType.IDENTITY) //genere auto valeur de cp    private Long id;    @NotEmpty //pas vide    @Size(min = 4, max = 40) //donner taille de chaine caractere    @Column(length = 50) //mapper une propriete d une entity a une colonne dans bd    private String nom;    @Temporal(TemporalType.DATE) //mapper une propriete d une entity a une type de donne temporel dans bd    @DateTimeFormat(pattern = "yyyy-MM-dd") //format date    private Date dateNaissance;    private boolean malade;    @DecimalMin("100") //nombre => valeur    private int score;    }